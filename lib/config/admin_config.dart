class AdminConfig {
  /// Client-side UI gate only.
  ///
  /// This constant is used to show/hide admin-only UI affordances.
  /// It is NOT a security boundary and must not be relied on to protect data.
  static const String adminEmail = 'anna.buzuk@gmail.com';
}
