name: Build Android AAB (Release Signed)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.9"
          channel: stable
          cache: true

      - name: Flutter doctor (for logs)
        run: flutter doctor -v

      - name: Install dependencies
        run: flutter pub get

      # --- Signing setup (THIS is what fixes the debug-signing / missing keystore issue) ---
      - name: Restore keystore + key.properties
        shell: bash
        run: |
          # Decode keystore from GitHub Secret into android/app (Gradle signing expects it there)
